/**
 * Enum for Http Methods
 * @enum {string}
 */

export enum HttpMethod {
  get = 'get',
  head = 'head',
  post = 'post',
  put = 'put',
  patch = 'patch',
  delete = 'delete',
  connect = 'connect',
  options = 'options',
}
